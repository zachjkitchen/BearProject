namespace Tessera
{
    /// <summary>
    /// Represents a particular face of a generic cell.
    /// The enum is empty - to work with directions, you need to either:
    /// * Use the methods on <see cref="ICellType"/>.
    /// * Cast to the enum specific to a given cell type, e.g. <see cref="CubeFaceDir"/>.
    /// </summary>
    public enum CellFaceDir
    {

    }
}